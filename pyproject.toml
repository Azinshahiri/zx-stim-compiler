[project]
name = "zx-stim-compiler"
version = "0.1.0"
description = "Research software for ZX-inspired optimization of Stim stabilizer and QEC circuits"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]